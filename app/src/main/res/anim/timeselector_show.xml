<?xml version="1.0" encoding="utf-8"?>
<set xmlns:android="http://schemas.android.com/apk/res/android" >

    <!-- 显示时逐渐从透明到不透明过渡，同时移动一小段距离 -->
    <translate
        android:duration="400"
        android:fromXDelta="-100%p"
        android:toXDelta="0" />

    <alpha
        android:duration="600"
        android:fromAlpha="0.0"
        android:toAlpha="1.0" />

</set>